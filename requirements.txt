# Every dependency must have a row in LICENSES/asset_manifest.csv before use.
# Pin exact versions before release -- MediaPipe landmark index semantics can drift
# between releases and silently invalidate every calibrated ROI.
#
# OpenCV: this project uses opencv-contrib-python, NOT opencv-python. MediaPipe requires
# the contrib distribution, and installing both puts two cv2 packages in the same
# directory where they clobber each other -- the resolved version then depends on install
# order, which is a repeatability bug. The contrib build must report
# "Non-free algorithms: NO"; tests/test_dependency_licenses.py enforces both properties.

numpy>=2.2.1            # BSD-3-Clause
opencv-contrib-python>=5.0.0.93   # Apache-2.0, nonfree OFF
scikit-image>=0.26.0       # BSD-3-Clause
mediapipe>=1.0.0          # Apache-2.0 (code and Face Landmarker weights, D3)
PyYAML>=6.0.3             # MIT
